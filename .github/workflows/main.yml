name: Build & Deploy Staging

on:
  push:
    branches: [main]
  workflow_dispatch: # Manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Staging AarnaLaw
        uses: appleboy/ssh-action@master

        with:
          host: ${{ secrets.HOST }} # Your server's IP addres
          username: ${{ secrets.USERNAME }} # Your server's username
          password: ${{ secrets.PASSWORD }} # Your server's password

          script: |
            cd /var/www/newwebsite
          # echo ${{ secrets.PASSWORD }} | sudo -S git config --global --add safe.directory /var/www/newwebsite
          #  echo ${{ secrets.PASSWORD }} | sudo -S git pull origin main
