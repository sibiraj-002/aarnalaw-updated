/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["docs.aarnalaw.com", "aarnalaw.com", "www.aarnalaw.com"], // Add your domain here
  },
};

export default nextConfig;
